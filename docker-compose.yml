services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bhos
      POSTGRES_PASSWORD: bhos_secret
      POSTGRES_DB: bhos_events
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
